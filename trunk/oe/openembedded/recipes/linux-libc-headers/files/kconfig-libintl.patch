--- linux-2.6.23/scripts/kconfig/Makefile~	2007-10-09 16:31:38.000000000 -0400
+++ linux-2.6.23/scripts/kconfig/Makefile	2010-09-27 14:52:55.213449300 -0400
@@ -151,6 +151,11 @@
 ifeq ($(KBUILD_HAVE_NLS),no)
 HOSTCFLAGS	+= -DKBUILD_NO_NLS
 endif
+# Add -lintl for building on cygwin
+HOST_OS := $(shell uname -o)
+ifeq (${HOST_OS},Cygwin)
+HOST_LOADLIBES += -lintl
+endif
 
 # generated files seem to need this to find local include files
 HOSTCFLAGS_lex.zconf.o	:= -I$(src)
