# Where to store sources
DL_DIR = "${HOME}/sources"
INHERIT += " rm_work "

# Make sure you have these installed
ASSUME_PROVIDED += "gdk-pixbuf-csource-native imagemagick-native librsvg-native"

# Which files do we want to parse:
BBFILES := "${HOME}/Projects/TeRK/oe/openembedded/recipes/*/*.bb"
BBMASK = ""
 
# What kind of images do we want?
IMAGE_FSTYPES += " ext2.gz "

# Set TMPDIR instead of defaulting it to $pwd/tmp
TMPDIR = "${HOME}/Projects/TeRK/oe/tmp"

# Make use of SMP and fast disks
PARALLEL_MAKE = "-j4"
BB_NUMBER_THREADS = "4"

# Machine type
MACHINE = "qwerk"

# Distribution name
DISTRO = "terkos"

# For building canadian SDK
SDK_ARCH = "i686"
SDK_OS = "mingw32"

# Use ARM instruction set
PREFERRED_ARM_INSTRUCTION_SET = "arm"
PREFERRED_ARM_INSTRUCTION_SET_armv4t   = "arm"
PREFERRED_ARM_INSTRUCTION_SET_armv5te  = "arm"
PREFERRED_ARM_INSTRUCTION_SET_armv5teb = "arm"
ARM_INSTRUCTION_SET = "arm"
